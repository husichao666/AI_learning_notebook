---
description: 适用于所有 Markdown 文件的排版规范，以及 Mermaid 图表的语法防错指南。当 AI 生成或编辑 .md 文件时自动触发。
alwaysApply: false
---
# Markdown & Mermaid 通用规范 (Markdown & Mermaid Guidelines)

在生成或编辑 Markdown 文件以及绘制 Mermaid 图表时，**必须**严格遵守以下排版与语法规范：

## 1. Markdown 排版规范
- **加粗解析问题**：在使用 `**加粗**` 语法时，如果加粗内容包含英文括号或与中文字符/标点紧密相连，请在 `**` 的**外侧两边各加一个空格**（例如：` 这是 **加粗内容(Bold)** 的示例 `），以确保各种 Markdown 解析器都能正确渲染加粗效果。

## 2. Mermaid 图表规范
- **Subgraph 命名规则**：在绘制 Mermaid 图表时，如果 `subgraph` 的名称包含空格或括号，必须使用 `subgraph ID [显示名称]` 的格式（例如：`subgraph RetrievalPhase [检索阶段 (Retrieval Phase)]`）。严禁直接使用 `subgraph 检索阶段 (Retrieval Phase)`，这会导致严重的解析错误。
